Also als nächstes, dann, wir sind immer noch da. Wir haben jetzt den Toggle aktiviert und sonstige Sachen. Jetzt tun Funktionen der kompletten Seite, wie du ja weißt. Wenn du schon gemacht hast, gut. Aber ansonsten, genau, also, wie es funktioniert, das ist ja auch das, ja, tatsächlich, wenn ich eine Alarmierung stufen, hier konfiguriere, bearbeite, zum Beispiel Native Push an oder aus mache und dann auch speichern kriege, dass es richtig gespeichert wird. Wir prüfen, dass das hier komplett sauber funktioniert. Wir prüfen, dass dann auch, wenn es hier oben auch richtig angezeigt wird, bei aktiver Alarmierung, und nicht ohne Native Push Alarm Button, sondern wichtig, dass das auch wirklich funktioniert, dass auch dann die Nachricht oben aufpoppt, ne, und dass dann natürlich auch das richtig getriggert wird. Dazu auch, genau, funktioniert und wird getriggert, das ist wichtig, und das ist korrekt, das ist auf jeden Fall okay. Also, wie die Seite jetzt auch funktionieren sollte, dazu erstmal analysiere und lese die Seite ganz genau, wie die Nachrichten funktionieren sollten, wie die verschiedenen Sections funktionieren sollten. Die Sections werden nicht angefasst, dass sie Golden State sind. Nur, dass sie jetzt richtig aktiv machen, halt schon hast, perfekt, halt, dass alle Funktionen funktionieren, okay. Besonders hier an den Alarmierungsstufen eigentlich, hier Toggle On, Toggle Off, Speichern, nicht speichern, wenn gespeichert und es getriggert wird, dann wirklich, dass es auch funktioniert, und dann hier angezeigt wird, okay. Jetzt, all die Sachen, die ich gesagt habe, schreibst jetzt auf und machst jetzt, pro Sache, die ich gesagt habe, zum Testen der Metrik, machst du jetzt pro Metrik 20 erfolgreiche Tests. Sogar wenn du 100 oder 200 oder 300 Tests benötigst, damit du 20 erfolgreiche Tests hast, ja. Du simulierst das in der Watchlist, oder kannst es nicht simulieren, da ist gar nichts drin, am besten Spot, weil es wird nicht gegeoblockt. Nutz einen Spottrend einfach, setz dir einen benachrichtigen, konfigurieren, und immer, wenn das getriggert wird, such dir eine Alarmierungsstufe aus, konfigurier die, dass Native Push aktiviert ist, und teste dann jeder einmal, einmal durch bitte, und dann sozusagen pro Metrik, die ich dann gesagt habe, 20 erfolgreiche Tests, okay. Pro Metrik, so, erfolgreiche, ja. Kein Playwriter, kein Playwriter im Chat und sowas, ja. Nur Backend, ja. Aktiver Alarmierung im Trendpreis Watchlist und benachrichtigen, konfigurieren, dann nicht angefasst, ja. Ist das klar? Jetzt ausführen, okay.